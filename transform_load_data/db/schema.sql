


CREATE TABLE compensation(code VARCHAR, geofips VARCHAR, geoname VARCHAR, timeperiod VARCHAR, cl_unit VARCHAR, unit_mult BIGINT, datavalue BIGINT, noteref VARCHAR, "table" VARCHAR, endpoint VARCHAR, topic VARCHAR);
CREATE TABLE population(code VARCHAR, geofips VARCHAR, geoname VARCHAR, timeperiod VARCHAR, cl_unit VARCHAR, unit_mult BIGINT, datavalue BIGINT, noteref VARCHAR, "table" VARCHAR, endpoint VARCHAR, topic VARCHAR);
CREATE TABLE wages_salary(code VARCHAR, geofips VARCHAR, geoname VARCHAR, timeperiod VARCHAR, cl_unit VARCHAR, unit_mult BIGINT, datavalue BIGINT, noteref VARCHAR, "table" VARCHAR, endpoint VARCHAR, topic VARCHAR);
CREATE TABLE real_gdp(code VARCHAR, geofips VARCHAR, geoname VARCHAR, timeperiod VARCHAR, cl_unit VARCHAR, unit_mult BIGINT, datavalue BIGINT, noteref VARCHAR, "table" VARCHAR, endpoint VARCHAR, topic VARCHAR);
CREATE TABLE employment(code VARCHAR, geofips VARCHAR, geoname VARCHAR, timeperiod VARCHAR, cl_unit VARCHAR, unit_mult BIGINT, datavalue BIGINT, noteref VARCHAR, "table" VARCHAR, endpoint VARCHAR, topic VARCHAR);
CREATE TABLE disposable_income(code VARCHAR, geofips VARCHAR, geoname VARCHAR, timeperiod VARCHAR, cl_unit VARCHAR, unit_mult BIGINT, datavalue DOUBLE, noteref VARCHAR, "table" VARCHAR, endpoint VARCHAR, topic VARCHAR);
CREATE TABLE gdp(code VARCHAR, geofips VARCHAR, geoname VARCHAR, timeperiod VARCHAR, cl_unit VARCHAR, unit_mult BIGINT, datavalue DOUBLE, noteref VARCHAR, "table" VARCHAR, endpoint VARCHAR, topic VARCHAR);
CREATE TABLE consumption_expenditures(code VARCHAR, geofips VARCHAR, geoname VARCHAR, timeperiod VARCHAR, cl_unit VARCHAR, unit_mult BIGINT, datavalue BIGINT, "table" VARCHAR, endpoint VARCHAR, topic VARCHAR);
CREATE TABLE personal_income(code VARCHAR, geofips VARCHAR, geoname VARCHAR, timeperiod VARCHAR, cl_unit VARCHAR, unit_mult BIGINT, datavalue DOUBLE, noteref VARCHAR, "table" VARCHAR, endpoint VARCHAR, topic VARCHAR);




